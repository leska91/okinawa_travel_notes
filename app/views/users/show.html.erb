<div class="contents row">
  <p><a href="#"><%= @nickname %></a>さんへメッセージ</p>
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @records.each do |record| %>
    <%= render partial: "records/record", locals: { record: record } %>
  <% end %>
</div>